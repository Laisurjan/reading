# 拆書共讀 —— RIA 課堂閱讀平台

## 專案概述

這是一個基於《極簡閱讀》RIA 拆書法的課堂即時互動網頁平台，供高中國文老師帶領全班進行深度閱讀活動。老師扮演「拆書家」角色，透過平台引導學生走完 R → I → A 的完整閱讀流程。

### 目標使用者
- **老師端**（拆書家）：高中國文老師，課前準備拆頁素材，課堂中即時監控學生進度、給予回饋
- **學生端**：技術高中學生（15-18 歲），閱讀動機偏低、習慣碎片化閱讀，需要明確的步驟引導

### 核心問題
學生依賴影音和碎片化閱讀，基本讀寫能力下降。平台要讓學生在課堂 45-50 分鐘內，透過結構化引導完成一次有品質的深度閱讀。

### 技術選型
- 網頁版（RWD，手機與電腦皆可使用）
- 前端框架：React + Tailwind CSS
- 建議用 Vite 建構
- 資料暫以 localStorage 或 in-memory state 模擬（原型階段）
- 未來可接後端 API

---

## RIA 方法論摘要（開發者必讀）

### 什麼是 RIA？

| 步驟 | 全名 | 說明 |
|------|------|------|
| **R** | Reading | 閱讀老師指定的文本片段（拆頁） |
| **I** | Interpretation | 用自己的話重述所讀內容 |
| **A1** | Appropriation 1 | 描述自己的相關經驗，與文本建立連結 |
| **A2** | Appropriation 2 | 規劃具體行動，將所學應用到生活中 |

### 課堂流程設計（45-50 分鐘）

```
課堂內完成：
R 閱讀（8-10 分鐘）
  → I 重述（12-15 分鐘）
    → 互看同學的 I（5 分鐘）
      → A1 經驗連結（12-15 分鐘）
        → 互看 A1 + 老師總結（5 分鐘）

回家作業：
A2 行動規劃（學生課後獨立完成）
```

### 各步驟的品質標準

這些標準要呈現在學生介面中，作為提示文字：

**I（重述）的 4 項標準：**
1. 詮釋：用自己的語言來寫，不是抄原文
2. 準確：解讀不能偏離原意
3. 清晰：邏輯要清楚簡練
4. 實用：試著轉化為具體操作或建議

**A1（相關經驗）的 4 項標準：**
1. 鮮活：必須是親眼所見、親耳所聞的事
2. 故事：敘事要完整，有起承轉合
3. 對應：要與原文或 I 的內容對應
4. 思考：能更深地幫助自己理解原文

**A2（行動應用）的 4 項標準：**
1. 目標：是否規劃了明確目標？
2. 行動：行動是否能拉近與目標的距離？
3. 關聯：是否使用了原文的方法？
4. 可控：自己能做得到嗎？

### 精準提問鷹架

平台需內建兩組提問框架，在學生書寫 I 和 A1 時可展開參考：

**框架一：追問前因後果**
- 前（前車可鑒）：為什麼這件事對我重要？是怎麼出現這問題的？
- 因（相因相生）：有哪些關於原因的假設？怎麼驗證或排除這些假設？
- 後（以觀後效）：若問題解決了，最好的結果是什麼？
- 果（自食其果）：如果我什麼都不做，會發生什麼？

**框架二：明確適用範圍**
- 適（適用其反）：有沒有不符合這觀點的例子？
- 用（使用條件）：要應用這觀點，需要具備什麼條件？
- 範（尋求範本）：其他人如何處理類似的問題？
- 圍（檢視圍牆）：不同意見和我的想法，真正的區別是什麼？

---

## 共讀模式

平台支援兩種模式，老師在建立任務時可自由選擇：

### 模式一：單一拆頁（全班讀同一段）
- 老師貼上一段 800-1500 字的文本片段
- 全班學生閱讀同一段文字
- 適合：一般課堂使用，討論聚焦

### 模式二：同主題多文本（學生選讀）
- 老師設定一個主題（如「幸福」「挫折」「自我」）
- 上傳 2-4 篇不同的短文，都圍繞同一主題
- 學生自選一篇進行 RIA
- I 和 A1 的互看跨文本進行（因為主題相同，仍可交流）
- 適合：增加學生選擇感，製造觀點碰撞

---

## 功能規格

### 一、老師端

#### 1.1 建立閱讀任務
- 輸入任務名稱
- 選擇共讀模式（單一拆頁 / 同主題多文本）
- 單一拆頁模式：
  - 填寫：文本標題、作者、出處
  - 貼上文本內容（支援基本格式：段落、粗體）
- 多文本模式：
  - 填寫主題名稱
  - 新增 2-4 篇文本（每篇各有標題、作者、出處、內容）
- 設定分組方式（可選）：
  - 隨機分組（指定每組人數）
  - 手動分組
  - 不分組（全班互看）
- 設定各步驟的時間建議（非強制計時，僅顯示參考）
- 產生「加入代碼」或「加入連結」，讓學生進入

#### 1.2 即時儀表板（課堂中使用）
- 全班進度總覽：以視覺化方式顯示每位學生目前在哪個步驟（R / I / A1 / A2）
  - 建議用色塊或進度條呈現
  - 可一眼看出誰還沒動、誰已完成
- 老師可即時「廣播」訊息給全班（如：「還有 5 分鐘，請完成 I」）
- 老師可控制流程節奏：
  - 「開放下一步」按鈕（如：全班一起從 R 進入 I）
  - 或設為自由進度（學生完成即可進入下一步）
- 點擊個別學生可查看其 I 和 A1 的內容
- 可對個別學生的回答給予簡短文字回饋或表情符號回饋（👍、💡、🔥、❓）

#### 1.3 課後回顧
- 查看全班所有 I、A1、A2 的完整紀錄
- 匯出功能（CSV 或列印格式）
- 標記優秀回答（可作為下次課堂的範例）

### 二、學生端

#### 2.1 加入任務
- 輸入加入代碼或掃 QR Code
- 輸入暱稱（或座號 + 姓名）
- 進入等待畫面，直到老師開始任務

#### 2.2 步驟一：R（閱讀）
- 全螢幕顯示文本內容，排版舒適易讀
- 多文本模式時：先顯示主題和文本選單，學生選擇一篇後進入閱讀
- 可調整字體大小
- 底部顯示「我讀完了」按鈕
- 閱讀時可畫重點（選取文字後標記），標記的文字在後續步驟中會顯示在側邊供參考

#### 2.3 步驟二：I（重述）
- 上方顯示原文（可收合），或顯示學生先前畫的重點
- 中央為文字輸入區，提示語：「用你自己的話，說說這段文字的重點是什麼？」
- 右側（或可展開的面板）顯示：
  - I 的 4 項品質標準（詮釋、準確、清晰、實用）作為提醒
  - 「提問鷹架」按鈕：點開顯示「追問前因後果」和「適用範圍」的引導問題，幫助學生思考
- 最少字數提示（建議 80 字以上，非硬性限制，但少於 30 字時溫和提醒）
- 完成後按「送出」

#### 2.4 互看環節（I 完成後）
- 顯示同組（或全班）同學的 I 重述內容
- 匿名或顯示暱稱（老師可設定）
- 學生可對他人的回答按「有啟發 💡」
- 此環節的目的是：看到別人怎麼理解同一段文字，刺激不同觀點
- 設計上要清楚區分「自己的回答」和「別人的回答」

#### 2.5 步驟三：A1（經驗連結）
- 提示語：「這段文字讓你想到自己生活中的什麼經歷？」
- 下方提供 3 個引導子問題（可選擇回答）：
  - 「讀完這段，你想到自己什麼經歷？」
  - 「你身邊有沒有人經歷過類似的事情？」
  - 「這讓你回想起什麼場景或對話？」
- 右側顯示 A1 的 4 項品質標準
- 同樣有「提問鷹架」可展開
- 完成後送出，進入 A1 互看環節（同 I 的互看機制）

#### 2.6 步驟四：A2（回家作業）
- 課堂結束後，學生可從「我的任務」進入
- 提示語：「讀完這段文字，你打算做什麼改變或嘗試？」
- 引導填寫：
  - 我的目標是：______
  - 我打算這樣做：______
  - 這和文章的關聯是：______
  - 我預計在 ____（時間）前完成
- 顯示 A2 的 4 項品質標準
- 送出後老師可在後台查看

### 三、精準提問鷹架（共用元件）

這是一個可在 I 和 A1 步驟中展開的輔助面板：

- 預設收合，點擊「需要靈感？試試這些問題 →」展開
- 展開後分兩個區塊：
  - 「追問前因後果」（前、因、後、果四個引導問題）
  - 「明確適用範圍」（適、用、範、圍四個引導問題）
- 每個問題旁有「用這個問題」按鈕，點擊後問題會插入到輸入框中作為思考起點
- 設計要輕量，不要喧賓奪主

---

## UI/UX 設計方向

### 整體風格
- **溫暖文學質感**：紙質色調背景（米白、暖灰），像翻開一本書的感覺
- **不要太遊戲化**：這是國文課，保持文學的沉靜感，但要有清晰的進度感
- **步驟感明確**：頂部永遠顯示 R → I → 互看 → A1 → 互看 → A2 的進度條，當前步驟高亮
- **手機優先**：學生大多用手機操作，確保手機版體驗流暢

### 色彩規劃
- 主色調：溫暖的墨綠或深棕（書卷氣）
- 輔助色：米白底色、淡金色點綴
- 步驟指示色：
  - R：沉穩藍
  - I：暖橙
  - A1：柔綠
  - A2：溫紅
- 避免螢光色或過於鮮豔的配色

### 排版原則
- 閱讀區的文字要大、行距要寬（學生要舒適閱讀）
- 輸入區要簡潔，不要太多干擾元素
- 品質標準和提問鷹架用收合面板，需要時才展開
- 老師端的儀表板要一目瞭然，在教室走動時用手機也能快速掌握

### 字型建議
- 正文閱讀：Noto Serif TC（思源宋體）增加文學感
- 介面文字：Noto Sans TC（思源黑體）保持清晰
- Google Fonts 載入即可

---

## 頁面結構

```
/                     → 首頁（選擇老師/學生身份）
/teacher/create       → 老師：建立新任務
/teacher/dashboard/:id → 老師：即時儀表板
/teacher/review/:id   → 老師：課後回顧
/join                 → 學生：輸入代碼加入
/session/:id          → 學生：閱讀任務主介面（R→I→A1→A2 流程）
/my-tasks             → 學生：我的任務列表（含待完成的 A2）
```

---

## 資料結構建議

```typescript
// 閱讀任務
interface Session {
  id: string;
  teacherId?: string; // 預留：未來多教師系統擴充用
  title: string;
  mode: "single" | "multi"; // 單一拆頁 or 多文本
  theme?: string; // 多文本模式的主題
  texts: Text[];
  joinCode: string;
  grouping: "random" | "manual" | "none";
  groupSize?: number;
  flowControl: "teacher" | "free"; // 老師控制節奏 or 自由進度
  currentStep: "waiting" | "R" | "I" | "I-share" | "A1" | "A1-share" | "A2";
  createdAt: Date;
}

// 文本（拆頁）
interface Text {
  id: string;
  title: string;
  author: string;
  source: string;
  content: string; // 800-1500 字
}

// 學生
interface Student {
  id: string;
  sessionId: string;
  name: string;
  group?: number;
  chosenTextId?: string; // 多文本模式時學生選的文本
}

// 學生回答
interface Response {
  id: string;
  studentId: string;
  sessionId: string;
  textId: string;
  step: "I" | "A1" | "A2";
  content: string;
  // A2 額外欄位
  a2Goal?: string;
  a2Action?: string;
  a2Connection?: string;
  a2Deadline?: string;
  highlights?: string[]; // R 步驟畫的重點
  submittedAt: Date;
}

// 老師回饋
interface Feedback {
  id: string;
  responseId: string;
  type: "emoji" | "text";
  emoji?: "👍" | "💡" | "🔥" | "❓";
  text?: string;
}

// 互看互動
interface Reaction {
  fromStudentId: string;
  toResponseId: string;
  type: "💡"; // 「有啟發」
}
```

---

## 開發優先順序

### Phase 1：核心流程（先做這個）
- [ ] 首頁（角色選擇）
- [ ] 老師建立任務（單一拆頁模式）
- [ ] 學生加入（代碼加入）
- [ ] 學生端 R → I → A1 完整流程
- [ ] 各步驟的品質標準提示
- [ ] 精準提問鷹架元件

### Phase 2：互動功能
- [ ] 互看環節（看同學的 I 和 A1）
- [ ] 「有啟發 💡」反應
- [ ] 老師即時儀表板（進度總覽）
- [ ] 老師廣播訊息
- [ ] 老師流程控制（開放下一步）

### Phase 3：完整功能
- [ ] 多文本模式
- [ ] 分組功能
- [ ] A2 回家作業區
- [ ] 老師回饋（emoji + 文字）
- [ ] 課後回顧與匯出
- [ ] 學生「我的任務」歷史

### Phase 4：優化
- [ ] QR Code 加入
- [ ] R 步驟畫重點功能
- [ ] 優秀回答標記與範例庫
- [ ] 手機端細節優化

### Phase 5（未來擴充，原型驗證後再規劃）
- [ ] 教師帳號登入系統
- [ ] 匯入班級與學生名單
- [ ] 多教師協作（共享拆頁素材庫）
- [ ] 跨班級 / 跨教師的互動與推廣機制
- [ ] 備註：資料結構已預留 teacherId 欄位，擴充時無需重構

---

## 開發注意事項

1. **原型階段不需要後端**：先用 React state + localStorage 模擬所有資料，跑通流程最重要
2. **手機體驗優先**：學生幾乎都用手機，老師可能也是拿手機在教室走動
3. **文字輸入體驗**：輸入框要夠大，手機鍵盤彈出時不要遮住內容
4. **步驟鎖定**：學生必須按順序完成，不能跳步（除非老師設定自由進度）
5. **鼓勵而非強制**：字數提示用溫和語氣（「再多寫一點會更好喔」），不要用紅字警告
6. **中文介面**：所有文字皆為繁體中文
7. **離線容錯**：學生填寫中途斷網不要丟失內容，定時自動儲存到 localStorage

---

## 示範用假資料

開發時可用以下文本測試：

### 拆頁範例一
- 標題：論幸福
- 作者：赫爾曼・赫塞
- 出處：《乞食行者》節選
- 內容：「幸福是一種怎樣的東西呢？關於幸福，已有無數的定義，而每一個人無分貴賤賢愚，他的心裡也有他自己的定義。幸福是主觀的，一件事情能使甲快樂，卻不能使乙快樂。我認為，幸福的本質，就在於你願意並且能夠做你自己。一個人若總是想做別人，模仿別人的生活，不管他獲得了多大的成功，他依舊不會幸福。因為他浪費掉的，正是自己的人生。」

### 拆頁範例二
- 標題：快思慢想
- 作者：丹尼爾・康納曼
- 出處：《快思慢想》第一章節選
- 內容：「我們的大腦有兩套思考系統。系統一是快速、直覺、不費力的；系統二是緩慢、刻意、需要專注的。當你看到 2+2 時，答案自動浮現，這是系統一。當你計算 17×24 時，你需要刻意集中注意力，這是系統二。問題在於，我們太常依賴系統一來做重要決定，卻以為自己在深思熟慮。」

---

## 附錄：RIA 理論背景

本平台的方法論來自趙周《極簡閱讀》一書。核心理念：

- **成人學習五定理**（馬爾科姆・諾爾斯）：自我導向、連結經驗、強調實踐、解決問題、內在驅動
- **拆書三步驟**：拆解知識 → 轉譯為自己的語言 → 實戰解決問題
- **濕貨比乾貨重要**：真正的學習不是整理重點，而是把知識和自己的經驗「弄濕」，產生連結
- **核心格言**：「未經思考的經驗不可靠，未經追問的問題不是真問題」
